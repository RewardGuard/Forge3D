# dependencies & build output
node_modules/
dist/
release/
build/*
!build/icon.png
!build/icon.icns
!build/entitlements.mac.plist
*.mcpb

# local tooling / personal
.claude/
.DS_Store
*.log

# user data never lives here, but belt & suspenders
*.config.json
.env
.env.*
!.env.example
server/cloud-bundle/manifest.json

# server runtime secrets/state — generated, must never be committed
.jwt-secret
server/**/data.json
