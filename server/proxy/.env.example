# Forge3D Cloud proxy — copy to .env and fill in ONE or more keys.
# .env is gitignored; keys live only on the server, never in the repo.
PORT=8787
RATE_PER_MIN=20

# Set at least one (priority: GLM > Groq > Gemini > Mistral > OpenRouter)
GLM_KEY=
GROQ_KEY=
GEMINI_KEY=
MISTRAL_KEY=
OPENROUTER_KEY=
